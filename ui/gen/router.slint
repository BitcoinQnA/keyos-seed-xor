// Generated by `slint-keyos-platform` DO NOT EDIT!

import { MainPagePage } from "../pages/page.slint";
import { CountPagePage } from "../pages/count/page.slint";
import { EntryPagePage } from "../pages/entry/page.slint";
import { FormatPagePage } from "../pages/format/page.slint";
import { LoadPagePage } from "../pages/load/page.slint";
import { OverviewPagePage } from "../pages/overview/page.slint";
import { PartsPagePage } from "../pages/parts/page.slint";
import { ResultPagePage } from "../pages/result/page.slint";
import { ReviewPagePage } from "../pages/review/page.slint";
import { TranscribePagePage } from "../pages/transcribe/page.slint";
import { VerifyPagePage } from "../pages/verify/page.slint";
import { WarnPagePage } from "../pages/warn/page.slint";
import { WordsPagePage } from "../pages/words/page.slint";
import { RouteState, RouteOption } from "internal.slint";

// Mount this component in your app to enable routing
export component Router inherits Rectangle {
    if (RouteState.active == RouteOption.MainPage): MainPagePage {
    }
    if (RouteState.active == RouteOption.CountPage): CountPagePage {
    }
    if (RouteState.active == RouteOption.EntryPage): EntryPagePage {
    }
    if (RouteState.active == RouteOption.FormatPage): FormatPagePage {
    }
    if (RouteState.active == RouteOption.LoadPage): LoadPagePage {
    }
    if (RouteState.active == RouteOption.OverviewPage): OverviewPagePage {
    }
    if (RouteState.active == RouteOption.PartsPage): PartsPagePage {
    }
    if (RouteState.active == RouteOption.ResultPage): ResultPagePage {
    }
    if (RouteState.active == RouteOption.ReviewPage): ReviewPagePage {
    }
    if (RouteState.active == RouteOption.TranscribePage): TranscribePagePage {
    }
    if (RouteState.active == RouteOption.VerifyPage): VerifyPagePage {
    }
    if (RouteState.active == RouteOption.WarnPage): WarnPagePage {
    }
    if (RouteState.active == RouteOption.WordsPage): WordsPagePage {
    }
}
