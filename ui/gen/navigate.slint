// Generated by `slint-keyos-platform` DO NOT EDIT!

export enum Animate {
    Forward,
    Backward,
    None,
}

struct NavigateOptions {
    // Replace the current route in the history stack
    replace: bool,
    
    // Animation to use when navigating
    animate: Animate,
}

export global Navigate {
    // Navigation state
    in property <bool> has-backward;
    in property <bool> has-forward;
    
    // Generic navigation callbacks
    callback backward();
    callback forward();
    
    callback backward-animate(Animate);
    callback forward-animate(Animate);
    
    callback return-home();
    callback return-home-animate(Animate);
    
    // Log the current navigation state
    callback debug();
    
    // Raw string navigation
    callback navigate(string, NavigateOptions);
    
    //
    // Type safe navigation callbacks
    //
    
    // 
    callback main-page(NavigateOptions);
    
    // /count
    callback count-page(NavigateOptions);
    
    // /entry
    callback entry-page(NavigateOptions);
    
    // /format
    callback format-page(NavigateOptions);
    
    // /load
    callback load-page(NavigateOptions);
    
    // /overview
    callback overview-page(NavigateOptions);
    
    // /parts
    callback parts-page(NavigateOptions);
    
    // /result
    callback result-page(NavigateOptions);
    
    // /review
    callback review-page(NavigateOptions);
    
    // /transcribe
    callback transcribe-page(NavigateOptions);
    
    // /verify
    callback verify-page(NavigateOptions);
    
    // /warn
    callback warn-page(NavigateOptions);
    
    // /words
    callback words-page(NavigateOptions);
}

export global RoutePath {
    out property <string> main-page : "";
    out property <string> count-page : "/count";
    out property <string> entry-page : "/entry";
    out property <string> format-page : "/format";
    out property <string> load-page : "/load";
    out property <string> overview-page : "/overview";
    out property <string> parts-page : "/parts";
    out property <string> result-page : "/result";
    out property <string> review-page : "/review";
    out property <string> transcribe-page : "/transcribe";
    out property <string> verify-page : "/verify";
    out property <string> warn-page : "/warn";
    out property <string> words-page : "/words";
}

